import { ratingScore } from 'karmayogi/helpers/rating-score';
import { module, test } from 'qunit';

module('Unit | Helper | rating score');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ratingScore([42]);
  assert.ok(result);
});
